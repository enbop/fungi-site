# WASI Runtime Note

This page is kept only as a compatibility stub.

The standalone WASI guide is obsolete for `v0.6.0-preview`. Wasmtime is now documented as one `RuntimeProvider` within the unified service workflow.

Start with these pages instead:

- [Services And Runtimes](service-manifests)
- [Runtime Examples](runtime-examples)

That guide covers:

- the new `RuntimeProvider` model
- the Docker-compatible container runtime and Wasmtime implementations
- `fungi service`, `fungi catalog`, `fungi access`, and `fungi peer`
- local pull/start flows and remote access flows